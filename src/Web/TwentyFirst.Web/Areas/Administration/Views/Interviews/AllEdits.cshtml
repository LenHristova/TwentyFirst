@model TwentyFirst.Common.Models.Interviews.InterviewAllEditsViewModel
@{
    ViewData["Title"] = Model.Title;
}
<h2>@ViewData["Title"]</h2>
<hr />
<p>Добавена от @Model.CreatorUserName на @Model.PublishedOnString</p>
@if (Model.Edits != null && Model.Edits.Any())
{
    <table class="table table-hover">
        <thead class="thead-light">
            <tr>
                <th>Редактор</th>
                <th>Редактирано</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var edit in Model.Edits)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => edit.EditorUserName)</td>
                    <td>@Html.DisplayFor(modelItem => edit.EditDateTimeString)</td>
                </tr>
            }
        </tbody>
    </table>

    <hr />
    @Html.PagedListPager((IPagedList)Model.Edits, pageNumber => Url.Action("AllEdits", new { pageNumber }),
        PagedListRenderOptionsBase.OnlyShowFivePagesAtATime)
}
else
{
    <p>Това интервю не е редактирано.</p>
}




